const userController = require('./user.controller');
const groupController = require('./group.controller');
const authController = require('./auth.controller');

module.exports = {userController, groupController, authController};
